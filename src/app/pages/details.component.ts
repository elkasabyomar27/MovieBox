import { Component, inject, signal } from '@angular/core';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { MovieService, Movie } from '../core/movie.service';
import { FavoritesService } from '../core/favorites.service';
@Component({imports:[RouterLink],template:`@if(loading()){<div class="state">Loading movie…</div>}@else if(movie()){<section class="detail"><a routerLink="/" class="back">← Back to discover</a><div class="layout"><img [src]="movie()!.image" [alt]="movie()!.title+' poster'"><div><p class="eyebrow">{{movie()!.genres.join(' · ')}}</p><h1>{{movie()!.title}}</h1><div class="meta"><span>★ {{movie()!.rating || 'Not rated'}}</span><span>{{movie()!.year}}</span><span>{{movie()!.runtime ? movie()!.runtime+' min' : 'Movie'}}</span></div><p class="summary">{{movie()!.summary}}</p><dl><div><dt>Language</dt><dd>{{movie()!.language || 'English'}}</dd></div><div><dt>Genres</dt><dd>{{movie()!.genres.join(', ')}}</dd></div></dl><button (click)="favorites.toggle(movie()!)" [class.saved]="favorites.has(movie()!.id)">{{favorites.has(movie()!.id)?'♥ Saved to my list':'♡ Add to my list'}}</button></div></div></section>}@else{<div class="state">This movie is unavailable. <a routerLink="/">Go home</a></div>}`,styles:`.detail{padding:42px clamp(22px,9vw,145px) 80px;min-height:70vh}.back{color:#c4c1c7;text-decoration:none;font-size:.9rem}.layout{margin-top:37px;display:grid;grid-template-columns:minmax(220px,340px) minmax(0,620px);gap:clamp(32px,6vw,86px);align-items:center}.layout>img{width:100%;aspect-ratio:2/3;object-fit:cover;border-radius:14px;box-shadow:18px 20px 0 #282630}.eyebrow{color:#f3ba38;letter-spacing:1.6px;font-size:.72rem;font-weight:700}.detail h1{font:clamp(2.7rem,5vw,4.8rem) Fraunces,serif;margin:12px 0}.meta{display:flex;gap:15px;color:#b9b6be;font-size:.9rem}.meta span:first-child{color:#f3ba38}.summary{color:#d0cdd3;line-height:1.8;font-size:1.05rem;margin:27px 0}dl{display:flex;gap:45px;margin:22px 0}dt{color:#777480;font-size:.76rem;text-transform:uppercase;letter-spacing:1px}dd{margin:5px 0;color:#f0edf1}button{background:#f3ba38;color:#17151d;border:0;border-radius:7px;padding:14px 19px;font-weight:800;cursor:pointer;font-size:.95rem}button.saved{background:#ed6c77}.state{text-align:center;padding:120px 20px;color:#bbb7bf}.state a{color:#f3ba38}@media(max-width:680px){.layout{grid-template-columns:1fr}.layout>img{width:210px}dl{gap:22px;flex-wrap:wrap}}`})
export class DetailsComponent { private route=inject(ActivatedRoute); private api=inject(MovieService); readonly favorites=inject(FavoritesService); movie=signal<Movie|null>(null); loading=signal(true); constructor(){this.api.detail(this.route.snapshot.paramMap.get('id')!).subscribe({next:x=>{this.movie.set(x);this.loading.set(false)},error:()=>this.loading.set(false)})} }
