import { Component, inject } from '@angular/core';
import { RouterLink } from '@angular/router'; import { FavoritesService } from '../core/favorites.service'; import { MovieCardComponent } from '../shared/movie-card.component';
@Component({imports:[RouterLink,MovieCardComponent],template:`<section><p class="eyebrow">YOUR COLLECTION</p><h1>My list</h1>@if(favorites.movies().length){<div class="grid">@for(movie of favorites.movies();track movie.id){<app-movie-card [movie]="movie"/>}</div>}@else{<div class="empty"><div>♡</div><h2>Nothing saved yet</h2><p>Keep the stories you want to return to in one place.</p><a routerLink="/">Explore titles</a></div>}</section>`,styles:`section{padding:62px clamp(22px,8vw,130px);min-height:65vh}.eyebrow{font-size:.7rem;letter-spacing:2px;font-weight:700;color:#f3ba38}h1{font:3rem Fraunces,serif;margin:8px 0 34px}.grid{display:grid;grid-template-columns:repeat(5,1fr);gap:20px}.empty{text-align:center;padding:70px 0;color:#aaa}.empty div{font-size:3rem;color:#f3ba38}.empty h2{font:2rem Fraunces,serif;color:#fff}.empty a{display:inline-block;background:#f3ba38;color:#18161d;text-decoration:none;padding:12px 18px;border-radius:7px;font-weight:800;margin-top:12px}@media(max-width:720px){.grid{grid-template-columns:repeat(2,1fr)}}`}) export class FavoritesComponent { readonly favorites=inject(FavoritesService); }
